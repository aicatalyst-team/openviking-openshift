apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - 01-namespace.yaml
  - 02-serving-runtime-embedding.yaml
  - 03-serving-runtime-vlm.yaml
  - 04-inference-service-embedding.yaml
  - 05-inference-service-vlm.yaml
  - 06-model-pvcs.yaml
  # Apply 07-download-jobs.yaml separately after PVCs are bound:
  #   oc apply -f manifests/openshift-ai/07-download-jobs.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: openviking
    includeSelectors: false
